# Chinese Study App

https://harryli0088.github.io/chinese-study-app/

## Adding items
1. Update `scripts/data.ts` with the item data and translation
2. Run `npx tsx processItem.ts` to add it to the webapp
3. Run `npx tsx generateCatalog.ts` to add it to the webapp catalog
4. Commit the changes